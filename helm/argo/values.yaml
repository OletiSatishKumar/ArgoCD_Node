replicaCount: 1

image:
  repository: satishosk/hello-argocd
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 3000

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: hello-argocd.local
      paths:
        - path: /
          pathType: Prefix
  tls: []

resources: {}
